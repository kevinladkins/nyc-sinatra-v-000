<!DOCTYPE html>
<html>
  <head>
    <title>Edit</title>
  </head>
  <body>
    <h1>Edit Figure</h1>
    <form action = "/figures/<%=@figure.id%>" method = "post">
      <label>Name: </label>
      <input type = "text" name = "figure[name]" id = "figure_name" value="<%=@figure.name%>">
      <br></br>
      <label>Landmark(s): </label>
      <br></br>
      <%Landmark.all.each do |l|%>
        <input type= "checkbox" name = "figure[landmark_ids][]" value="<%=l.id%>" id="landmark_<%=l.id%>" <%= 'checked' if @figure.landmark_ids.include?(l.id)%>><%=l.name%></input>
      <%end%>
      <br></br>
      <label>And/Or Create A New Landmark: </label>
        <input type = "text" name = "landmark[name]" id = "new_landmark" placeholder = "name">
        <br></br>
      <label>Title(s): </label>
      <br></br>
      <%Title.all.each do |t|%>
        <input type= "checkbox" name = "figure[title_ids][]" value="title_<%=t.id%>" id="<%=t.id%>" <%= 'checked' if @figure.title_ids.include?(t.id)%>><%=t.name%></input>
      <%end%>
      <br></br>
      <label>And/Or Create A New Title: </label>
        <input type = "text" name = "title[name]" id = "new_title" placeholder = "title">
        <br></br>
        <input type = "submit" value = "Edit Figure">
    </form>
  </body>

</html>
